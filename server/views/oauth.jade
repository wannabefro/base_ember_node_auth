html
  head
  body
    script(type='text/javascript').
      if (typeof(success) !== undefined){
        window.opener.App.ExternalConnector.trigger('externalAuthenticationSucceeded',{access_token: !{JSON.stringify(access_token)} })
      } else {
        window.opener.App.ExternalConnector.trigger('externalAuthenticationFailed', 'access not granted!');
      }
      window.close()

